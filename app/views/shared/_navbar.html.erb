<div id="navbar">
  <div class="main-wrapper">
    <div>
      <h1 class="bold-secondary"><%= link_to "HOME IMPROVEMENT", root_path %></h1>
    </div>
    <div>
      <% if current_user %>
        <% unless action_name == "new" && controller_name == "projects" %>
          <h3 class="text-center bold-secondary"><%= link_to "New Project", new_project_path %></h3>
          <p></p>
        <% end %>
        <h3 class="text-center bold-secondary"><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></h3>
      <% else %>
        <h3 class="text-center bold-secondary"><%= link_to "Sign In", new_user_session_path %></h3>
        <p>or</p>
        <h3 class="text-center bold-secondary"><%= link_to "Sign Up", new_user_registration_path %></h3>
      <% end %>
    </div>
  </div>
</div>